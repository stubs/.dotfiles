#!/bin/bash

# Use Homebrew's python and usr/local/bin before other paths
export PATH=/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin

export TERM='xterm'

# Adding custom colors to bash_profile
export PS1="[\T] \[\033[36m\]\u\[\033[m\]@\[\033[32m\]\h:\[\033[33;1m\]\w\[\033[m\]\$ "
export CLICOLOR=1
export LSCOLORS=ExFxBxDxCxegedabagacad

# Highlight section titles in manual pages
export LESS_TERMCAP_md="${yellow}"

# Don’t clear the screen after quitting a manual page
export MANPAGER='less -X'

# Make Python use UTF-8 encoding for output to stdin, stdout, and stderr.
export PYTHONIOENCODING='UTF-8';

# java
export JAVA_HOME=$(/usr/libexec/java_home -v 1.8)

# virtualenvs
export WORKON_HOME=$HOME/.virtualenvs

# work vs home
if [ "$HOSTNAME" = "ABT-AG-h03647.local" ]; then
    # Virtualenvwrapper stuff
    export PROJECT_HOME=$HOME/titan
    export VIRTUALENVWRAPPER_PYTHON=/usr/local/bin/python2

    # pyspark use Jupyter for dev
    export SPARK_HOME=/usr/local/lib/python2.7/site-packages/pyspark
    export PYSPARK_DRIVER_PYTHON=jupyter
    export PYSPARK_DRIVER_PYTHON_OPTS='notebook'
fi

